import { helper } from '@ember/component/helper';

function assertNoMirageResource([value]) {
  debugger;
  console.log("this is a test");
  return value;
}

export default helper(assertNoMirageResource);
